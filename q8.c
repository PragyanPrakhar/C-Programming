//Write a program that prints the floating point value in exponential format correct to two decimal place
#include <stdio.h>
int main(){
    float a;
    printf("enter the value of a");
    scanf("%f",&a);
    printf("the exponential foormat is %e",a);
    return 0;

}